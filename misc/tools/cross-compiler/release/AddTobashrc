# ~/.bashrc: executed by bash(1) for non-login shells.

#Add these lines in .bashrc to enable bash to locate Cross compiler executables

#Paths to executables
export LD_LIBRARY_PATH=$(pwd)/lib:${LD_LIBRARY_PATH}

#this is where some binaries are located
export PATH=$PATH:$(pwd)/bin:${LD_LIBRARY_PATH}



