This is the boot image for the successful boot of Ajit_linux
onto the AJIT platform with FPGA + flash + DRAM.

compile.sh
	compile script.

boot_loader_plus_kernel_mmap/
	contents generated by the kernel compile
	script sequence 

	   GeneratememMapForAjit.sh
	   vmLinuzToMemmapAtf0004000.sh

	after they are run in the directory ../


flash_contents/
	bootstrap image mmap and bin files.

bootstrap_bin_file/
	The bootstrap bin file used in last
	successful linux boot.
	
