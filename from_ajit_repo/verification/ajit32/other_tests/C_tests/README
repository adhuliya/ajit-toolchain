To generate a memory map from a single C program file , run
	
	$ cToSparc.py <C file>

The memory map is created in ./output/<C file name>_memmap.txt
To run this workload on the processor model, run
	
	$ AJIT_cpu_testbench <memory map file>

For more detailed compilation/linking options,
see 	$ compileToSparc.py -h
